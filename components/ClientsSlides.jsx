import style from '@styles/ClientsSlides.module.scss'
import Image from 'next/image'

const ClientsSlides = () => {
  return (
    <div className={style.container}>
      <div className={`${style.slide} ${style.osm}`}>
      </div>
      <div className={`${style.slide} ${style.runbhumi}`}>
      </div>
      <div className={`${style.slide} ${style.carnival}`}>
      </div>
      <div className={`${style.slide} ${style.kulfi}`}>
      </div>
      <div className={`${style.slide} ${style.newpune}`}>
      </div>
      {/* <div className={`${style.slide} ${style.modak}`}>
      </div>
      <div className={`${style.slide} ${style.punestudio}`}>
      </div>
      <div className={`${style.slide} ${style.right}`}>
      </div> */}
      {/* <div className={`${style.slide} ${style.delilah}`}>
      </div>
      <div className={`${style.slide} ${style.promax}`}>
      </div>
      <div className={`${style.slide} ${style.sadguru}`}>
      </div>
      <div className={`${style.slide} ${style.ved}`}>
      </div>
      <div className={`${style.slide} ${style.jwellers}`}>
      </div>
      <div className={`${style.slide} ${style.akrealty}`}>
      </div>
      <div className={`${style.slide} ${style.tea}`}>
      </div>
      <div className={`${style.slide} ${style.pvg}`}>
      </div>
      <div className={`${style.slide} ${style.epb}`}>
      </div> */}
    </div>
  )
}

export default ClientsSlides  